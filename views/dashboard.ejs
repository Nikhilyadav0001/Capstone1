<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard | Overview</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #f16363;
      --bg-gradient: radial-gradient(circle at top left, #1e1b4b, #0f172a);
      --glass: rgba(255, 255, 255, 0.03);
      --glass-border: rgba(255, 255, 255, 0.08);
      --text-main: #f8fafc;
      --text-dim: #94a3b8;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Inter', sans-serif; }

    body {
      background: var(--bg-gradient);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-main);
    }

    .dashboard-container {
      width: 100%;
      max-width: 500px;
      padding: 20px;
      animation: zoomIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    @keyframes zoomIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }

    .profile-card {
      background: var(--glass);
      backdrop-filter: blur(30px);
      -webkit-backdrop-filter: blur(30px);
      border: 1px solid var(--glass-border);
      border-radius: 32px;
      padding: 40px;
      text-align: center;
      box-shadow: 0 40px 100px rgba(0,0,0,0.4);
      position: relative;
      overflow: hidden;
    }

    /* Decorative element */
    .profile-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; width: 100%; height: 4px;
      background: linear-gradient(90deg, var(--primary), #a855f7);
    }

    .avatar-wrapper {
      position: relative;
      width: 110px;
      height: 110px;
      margin: 0 auto 24px;
    }

    .avatar {
      width: 100%;
      height: 100%;
      border-radius: 30px;
      background: linear-gradient(135deg, var(--primary), #a855f7);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 42px;
      font-weight: 700;
      color: white;
      box-shadow: 0 15px 30px rgba(99, 102, 241, 0.3);
      border: 3px solid rgba(255,255,255,0.1);
      overflow: hidden;
    }

    .avatar img { width: 100%; height: 100%; object-fit: cover; }

    .status-badge {
      position: absolute;
      bottom: -5px;
      right: -5px;
      background: #22c55e;
      width: 20px;
      height: 20px;
      border-radius: 50%;
      border: 3px solid #161b33;
    }

    h1 { font-size: 28px; font-weight: 700; margin-bottom: 4px; }
    .email-label { color: var(--text-dim); font-size: 15px; margin-bottom: 32px; display: block; }

    .stats-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 12px;
      margin-bottom: 32px;
    }

    .stat-item {
      background: rgba(255, 255, 255, 0.03);
      padding: 16px 20px;
      border-radius: 16px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border: 1px solid var(--glass-border);
      transition: background 0.3s;
    }

    .stat-item:hover { background: rgba(255, 255, 255, 0.06); }

    .stat-label { color: var(--text-dim); font-size: 13px; text-transform: uppercase; letter-spacing: 0.5px; }
    .stat-value { font-weight: 600; color: var(--text-main); font-size: 15px; }

    .method-tag {
      padding: 4px 10px;
      background: rgba(99, 102, 241, 0.15);
      color: #a5b4fc;
      border-radius: 8px;
      font-size: 12px;
      font-weight: 600;
    }

    .btn-logout {
      display: block;
      width: 100%;
      padding: 16px;
      background: rgba(239, 68, 68, 0.1);
      color: #fca5a5;
      text-decoration: none;
      border-radius: 16px;
      font-weight: 600;
      transition: all 0.3s ease;
      border: 1px solid rgba(239, 68, 68, 0.2);
    }

    .btn-logout:hover {
      background: #ef4444;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(239, 68, 68, 0.2);
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <div class="profile-card">
      <div class="avatar-wrapper">
        <div class="avatar">
          <% if (user.avatar) { %>
            <img src="<%= user.avatar %>" alt="avatar" />
          <% } else { %>
            <%= user.name.charAt(0).toUpperCase() %>
          <% } %>
        </div>
        <div class="status-badge"></div>
      </div>

      <h1><%= user.name %></h1>
      <span class="email-label"><%= user.email %></span>

      <div class="stats-grid">
        <div class="stat-item">
          <span class="stat-label">Member Status</span>
          <span class="stat-value"><span class="method-tag">Verified</span></span>
        </div>
        <div class="stat-item">
          <span class="stat-label">Authentication</span>
          <span class="stat-value"><%= user.googleId ? 'Google Cloud' : 'Native Auth' %></span>
        </div>
        <div class="stat-item">
          <span class="stat-label">Joined On</span>
          <span class="stat-value"><%= new Date(user.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></span>
        </div>
      </div>

      <a href="/logout" class="btn-logout">Sign Out of Session</a>
    </div>
  </div>
</body>
</html>